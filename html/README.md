# FANGYU.EXE HTML 交付文件

这是可直接交付或部署的 HTML 文件夹。

- `index.html`：完整网站，已内嵌 CSS、JavaScript 和项目占位数据。

## 使用方法

1. 将整个 `html` 文件夹上传至任意静态托管服务（Vercel、Netlify、GitHub Pages 等），并将发布目录设为 `html`；或
2. 直接双击 `index.html` 用浏览器打开。

不需要 Node.js、构建命令或额外的 `src` 文件夹。
